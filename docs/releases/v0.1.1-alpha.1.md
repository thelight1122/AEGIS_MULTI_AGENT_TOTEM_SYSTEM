# AEGIS Totem v0.1.1-alpha.1

Status: Draft release notes. Do not publish until Tracey explicitly authorizes the publication gate.

## Summary

AEGIS Totem is a free, local-first developer tool for append-only repository continuity and multi-agent coordination. It gives a repository a Root Totem, Folder Totems, and one lane per human, agent, model, or instance so parallel work can coordinate without a copy/paste bridge.

## Included In This Alpha

- TypeScript CLI with `start`, structured Start JSON, generated Root Totem tutorial for new AI agents, `init`, lane creation, lane messaging, Folder Totem creation, Folder Totem appends, `status`, filtered list, read commands, analytics, validation, readiness doctor with JSON output, MCP config generation, and optional local validation hook installation.
- Append-only write helper with lock-directory serialization for concurrent agent writes.
- MCP stdio server exposing start, read, append, status, list, analytics, validation, and doctor tools for AI coding agents.
- MCP client examples for Codex, Cursor, Claude Desktop, Cline, Windsurf, and other stdio clients.
- VS Code extension adapter with a dedicated Activity Bar icon, visible sidebar command rows, `Initialize System`, and `AEGIS Totem: Start`, backed by a bundled alpha CLI runtime for existing-repo seeding, automatic Doctor readiness after Start, browsing Totems and lanes, viewing read-only List, Analytics, structured Doctor, and Live Test Checklist panels, sending lane messages, and appending Folder Totem updates.
- Local install QA, MCP QA, VS Code package QA, docs QA, release preflight, CI, release checklist, security policy, and quickstart documentation.

## Installation During Alpha

Until npm publication is explicitly approved, use the repository build:

```bash
npm install
npm run alpha:check
npm link
```

After npm publication is explicitly approved, the intended install command is:

```bash
npm install -g aegis-totem
```

## VS Code Alpha Install

Build and install the local VS Code package:

```bash
npm run vscode:qa
code --install-extension dist/aegis-totem-vscode-0.1.1.vsix
```

On Windows, install the `.vsix` from inside VS Code. Double-clicking the file can route it to the Visual Studio VSIX Installer and produce a false install failure.

This alpha includes the initial AEGIS Totem VS Code icon at `vscode-extension/media/aegis-totem-icon.png`.

## Publication Gates

Publication is intentionally separate from build readiness.

- `npm publish` requires explicit authorization.
- Creating the GitHub Release requires explicit authorization.
- Attaching the `.vsix` to a GitHub Release requires explicit authorization.
- Publishing to the VS Code Marketplace or Open VSX requires explicit authorization.

## Preflight Observations

Observed on 2026-09-24 during final alpha release-readiness:

- `npm ci` completed from the committed lockfile and reported 0 vulnerabilities.
- Initial `npm audit` found dev-only Vitest/Vite-chain advisories; production dependencies were clean with `npm audit --omit=dev`.
- Upgraded `vitest` to `^4.1.11`, which cleared `npm audit`.
- Hardened append lock acquisition for transient Windows `EPERM` during concurrent lock-directory creation/removal.
- `npm test` passed with 14 test files and 34 tests under Vitest 4.1.11.
- `npm run alpha:check` passed from `main` after the MCP Start and VSIX ZIP QA fixes.
- `npm pack --dry-run` reported 19 package files, about 13.0 kB packed and 46.1 kB unpacked.
- `npm run vscode:qa` produced `dist/aegis-totem-vscode-0.1.1.vsix` with the bundled alpha CLI runtime, 12 commands, 1 Activity Bar view, and a packaged VSIX bundled-CLI smoke test.
- `npm run release:preflight` passed for `aegis-totem@0.1.1` and `v0.1.1-alpha.1`.
- GitHub Actions CI passed on `main` for commit `a08ab49`.
- Local VS Code VSIX install succeeded with `code --install-extension dist/aegis-totem-vscode-0.1.1.vsix --force`, and `code --list-extensions --show-versions` listed `thelight1122.aegis-totem-vscode@0.1.1`.
- `npm view aegis-totem name version --json` returned `E404`, indicating the package name was not found in the npm registry at that moment.
- `gh release view v0.1.1-alpha.1 --repo thelight1122/AEGIS_MULTI_AGENT_TOTEM_SYSTEM` returned `release not found` at that moment.

These are time-sensitive observations. Re-check them immediately before any publication action.
